spring.application.name=backend

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5433/votacao_performance
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Context Path
server.servlet.context-path=/api

# Server
server.port=8081

#./mvnw spring-boot:run "-Dspring-boot.run.profiles=performance"
#docker run --rm -i grafana/k6 run - < performance/voting.js
#docker run --rm -i -v "/$PWD:/k6" -w //k6 grafana/k6 run - < performance/voting.js

